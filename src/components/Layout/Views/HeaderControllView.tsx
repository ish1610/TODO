import styled from "@emotion/styled";

const HeaderControll = styled.div`
  button {
    margin-right: 20px;
  }
`;

const HeaderControllView = () => {
  return <HeaderControll></HeaderControll>;
};

export default HeaderControllView;
