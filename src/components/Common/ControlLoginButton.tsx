import React from "react";
import { IControlLoginButton } from "./type";

const ControlLoginButton = ({ content }: IControlLoginButton) => {
  return <ControlLoginButton content={content} />;
};

export default ControlLoginButton;
